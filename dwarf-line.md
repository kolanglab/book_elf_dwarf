# 行番号情報 ―― アドレスとソース行の対応表

「クラッシュしたのは `main.c` の 17 行目」とデバッガが言えるのは、アドレスとソース行の対応表があるからです。その対応表が `.debug_line` セクションに格納されています。素朴に「アドレス → 行番号」の巨大な表を作ると膨大になるため、DWARF は**状態機械を駆動するバイトコード**という、独特で効率的な方式を採ります。この章では、その「行番号プログラム」の仕組みを追います。DWARF の設計思想 ―― 冗長な表を、それを生成する小さなプログラムに置き換える ―― が最もよく表れた部分です。

## 行番号表という考え方

理想的には、命令のアドレスごとに「どのファイルの、何行目の、何文字目か」を記した表があればよいわけです。概念的には次のような行が並びます。

| アドレス | ファイル | 行 | 列 | 文の先頭か |
|---|---|---|---|---|
| 0x1149 | main.c | 5 | 0 | yes |
| 0x1151 | main.c | 6 | 0 | yes |
| 0x115a | main.c | 7 | 0 | yes |
| 0x1160 | main.c | 7 | 0 | no（行末） |

この表があれば、停止アドレス 0x1153 がどの行かは、「0x1153 以下で最大のアドレスを持つ行」を探して 6 行目、と求められます。逆に「6 行目の機械語はどこから」も引けます。問題は、この表が命令の数だけ行を持ち、巨大になることです。

そこで DWARF は、この表を**そのまま格納せず**、表を 1 行ずつ生成していく小さなプログラム（バイトコード）を格納します。デバッガはそのプログラムを実行することで、必要に応じて表を再構成します [](#cite:dwarf2017)。

## 行番号状態機械

このバイトコードを実行する仮想マシンを**行番号状態機械** (line number state machine) と呼びます。状態機械は、表の「いま作りつつある 1 行」に相当するレジスタ群を持ちます。主なものは次のとおりです。

| レジスタ | 意味 | 初期値 |
|---|---|---|
| `address` | 機械語アドレス | 0 |
| `file` | ファイル番号 | 1 |
| `line` | 行番号 | 1 |
| `column` | 列番号 | 0 |
| `is_stmt` | 文の先頭（ブレークポイント候補）か | 既定値 |
| `end_sequence` | アドレス列の終端か | false |

バイトコードの各命令（オペコード）は、これらのレジスタを少しずつ変化させます。そして特定の命令が来たときに、「いまのレジスタの値を、表の 1 行として**出力 (append)** せよ」という意味になります。こうして、命令を順に実行すると表の行が次々と吐き出されていくのです。

> [!NOTE]
> この方式の利点は、**増分だけを記録すればよい**ことです。隣り合う命令は、たいていアドレスが数バイト増え、行が 0〜1 行増えるだけです。だから「アドレスを 7 進めて、行を 1 増やして、1 行出力」といった小さな増分命令の列で表せます。絶対値を毎回書くより、ずっとコンパクトになります。これは前章の LEB128 と同じ「差分は小さいことが多い」という観察に基づく設計です。

## 3 種類のオペコード

行番号プログラムのオペコードは、3 種類に分かれます。

**特別オペコード** (special opcode): 1 バイトで、「アドレスを少し進め、行を少し増減し、1 行出力する」という最頻出の操作をまとめて行います。行番号プログラムの大半はこれです。1 バイトの値から、アドレスの増分と行の増分が計算式で割り出されます。計算には、ヘッダで定義される `line_base`・`line_range`・`minimum_instruction_length` といったパラメータが使われます。たとえば「アドレスを 1 進め、行を 1 増やし、出力」という典型操作が、たった 1 バイトで表せます。

**標準オペコード** (standard opcode): 0〜12 番の決まった命令です。代表的なものを挙げます。

| オペコード | 動作 |
|---|---|
| `DW_LNS_copy` | 現在のレジスタを 1 行出力する |
| `DW_LNS_advance_pc` | アドレスを指定量進める |
| `DW_LNS_advance_line` | 行番号を指定量増減する（SLEB128） |
| `DW_LNS_set_column` | 列番号を設定する |
| `DW_LNS_negate_stmt` | `is_stmt` を反転する |
| `DW_LNS_set_file` | ファイル番号を設定する |

**拡張オペコード** (extended opcode): 0 で始まり、長さと副オペコードを持つ、可変長の命令です。重要なものは 2 つです。`DW_LNE_set_address` は、アドレスレジスタに**絶対アドレス**を設定します（増分では効率が悪い、関数の先頭などで使う）。`DW_LNE_end_sequence` は、`end_sequence` を立てて 1 行出力し、状態機械をリセットします ―― これが連続したアドレス列（典型的には 1 つの関数）の終わりを示します。

## 行番号プログラムを実行してみる

具体的なバイトコード列を、手で実行して表を組み立ててみましょう。次のようなプログラムを考えます（左がオペコード、右が動作）。

```
DW_LNE_set_address 0x1149   ; address = 0x1149
DW_LNS_copy                 ; → 出力: (0x1149, line 1)  ※実際は set_file 等の後
special (addr+=8, line+=4)  ; address=0x1151, line=5 → 出力: (0x1151, line 5)
special (addr+=9, line+=1)  ; address=0x115a, line=6 → 出力: (0x115a, line 6)
DW_LNS_advance_pc 6         ; address=0x1160
DW_LNE_end_sequence         ; → 出力: (0x1160, end) して終了
```

これを実行すると、次の表が再構成されます。

| アドレス | 行 | 備考 |
|---|---|---|
| 0x1149 | 1 | |
| 0x1151 | 5 | |
| 0x115a | 6 | |
| 0x1160 | — | end_sequence（この関数の終端） |

たった数バイトのプログラムから、4 行の表が生成されました。停止アドレスが 0x1156 なら、「0x1156 以下で最大」は 0x1151 なので 5 行目、と判定できます。`end_sequence` の行は「ここから先はもうこの関数のコードではない」という番兵で、アドレス範囲の上限を与えます。実際のコンパイラ出力では特別オペコードが大半を占め、1 バイトずつで表が編まれていきます。

## 行番号ヘッダとファイル名

`.debug_line` の各単位もヘッダから始まり、そこに状態機械のパラメータ（`line_base` など）と、**ファイル名・ディレクトリのテーブル**が含まれます。`file` レジスタの番号は、このテーブルを引いて実際のファイル名になります。DWARF 第 5 版では、このファイル名テーブルの形式が改良され、`.debug_line_str` への参照を使うなど、より柔軟になりました [](#cite:dwarf2017)。

`readelf --debug-dump=decodedline` を使うと、ツールが状態機械を実行して**復元済みの表**を表示してくれます。

```
$ readelf --debug-dump=decodedline a.out
CU: ./main.c:
File name      Line number    Starting address
main.c                   5            0x1149
main.c                   6            0x1151
main.c                   7            0x115a
main.c                   8            0x1160
```

この表こそ、`addr2line` コマンドや、クラッシュ時のスタックトレースで「ファイル名:行番号」を出すための種です。`addr2line -e a.out 0x1155` と打つと、ツールはこの表を引いて `main.c:5` と答えます。第 III 部のハンズオンでは、この `addr2line` 相当の処理を自分で書きます。

## is_stmt とブレークポイント

最後に `is_stmt` レジスタに触れておきます。これは「その行が**文の始まり**として、ブレークポイントを置くのにふさわしい場所か」を示すフラグです。1 つのソース行が複数の機械語アドレスにまたがるとき、デバッガはどこで止めるべきか迷います。`is_stmt` が立っているアドレスを選べば、その行の「自然な開始点」で止められます。

> [!TIP]
> `break main.c:10` のような行ブレークポイントが、実は「10 行目の先頭アドレス」に正確に置かれるのは、この `is_stmt` 付きの行番号表のおかげです。コンパイラは、文の区切りとして自然なアドレスに `is_stmt` を立てておきます。デバッガはそれを尊重することで、変数がまだ初期化されていない中途半端な位置ではなく、ユーザーが期待する位置で停止できるのです。

ここまでで、DWARF の主要な構成要素 ―― DIE の木、型、場所式、行番号情報 ―― を一通り見てきました。これらを組み合わせれば、アドレスから行へ、行からアドレスへ、変数から値へとたどれます。しかし、それは「うまくいくとき」の話です。次章では、本書が最も重視する問いに正面から向き合います ―― **DWARF から何が分かり、何が分からないのか**。とくに、最適化された現実のコードで、なぜデバッグがしばしば期待を裏切るのかを掘り下げます。
