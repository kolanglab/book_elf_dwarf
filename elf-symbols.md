# シンボルと再配置 ―― 名前とアドレスを結ぶ

第 1 章で、オブジェクトファイルは「`printf` の本体がどこにあるか分からないまま」出力され、リンカがそれを解決する、と述べました。この「名前」と「アドレス」を仲立ちする仕組みが、**シンボル** (symbol) と**再配置** (relocation) です。この章は、ELF 部の中でも最も「リンカの心臓部」に近いところです。少し抽象的になりますが、ここを理解するとコンパイル・リンクという作業の意味が一段深く分かります。

## シンボルとは何か

[シンボル](#index:シンボル)とは、おおざっぱに言えば「**名前の付いたアドレス**」です。プログラム中の関数や変数 ―― たとえば `main`、`printf`、グローバル変数 `counter` ―― は、それぞれメモリ上のどこかに置かれます。その「名前」と「置かれた場所（アドレス）」の対応表が、**シンボルテーブル** (symbol table) です。

なぜこんな表が必要なのでしょうか。コンパイラは 1 つの `.c` ファイルを単独でコンパイルします。`a.c` の中で `b.c` で定義された関数 `helper` を呼んでいても、コンパイラは `helper` のアドレスを知りません。そこでコンパイラは、「`helper` という名前の関数を呼ぶ」という情報だけを残し、アドレスは空欄にしておきます。後でリンカが、すべての `.o` のシンボルテーブルを突き合わせ、`helper` の定義を `b.o` の中に見つけ、空欄を埋めるのです [](#cite:levine2000)。

つまりシンボルには、大きく 2 つの立場があります。

- **定義済みシンボル** (defined symbol): その名前の実体がこのファイルの中にある（`helper` を定義した `b.o` から見た `helper`）。
- **未定義シンボル** (undefined symbol): その名前を使っているが、実体は他のファイルにある（`helper` を呼ぶ `a.o` から見た `helper`）。

リンクとは、本質的には「すべての未定義シンボルに対応する定義済みシンボルを見つけ、結びつける」作業だと言えます。リンカの内部動作をさらに深く追いたい読者には、実装者の視点から書かれた Ian Lance Taylor の連載 [](#cite:taylor2007) が、本書の範囲を超えて詳しい解説を与えてくれます。

## シンボルエントリ Elf64_Sym

シンボルテーブル（`.symtab` セクション）は、`Elf64_Sym` という 24 バイトの構造体の配列です [](#cite:tis1995)。

```c
typedef struct {
    uint32_t      st_name;   /* シンボル名（.strtab へのオフセット） */
    unsigned char st_info;   /* 型と束縛（bind）を詰めた 1 バイト */
    unsigned char st_other;  /* 可視性 */
    uint16_t      st_shndx;  /* 定義先セクション番号 */
    uint64_t      st_value;  /* 値（多くはアドレス） */
    uint64_t      st_size;   /* シンボルが占めるサイズ */
} Elf64_Sym;
```

ここでも `st_name` はオフセットで、前章の文字列テーブル（`.symtab` の場合は `.strtab`）を引いて名前を得ます。`st_value` がそのシンボルのアドレス、`st_size` はサイズ（関数ならコード長、変数ならバイト数）です。

`st_shndx` は「このシンボルがどのセクションで定義されているか」をセクション番号で示します。ここに特別な値が入ることがあり、これがシンボルの立場を表します。

- `SHN_UNDEF`（0）: **未定義**。実体は他のファイルにある。
- `SHN_ABS`（0xfff1）: 絶対値。再配置の影響を受けない定数。
- `SHN_COMMON`（0xfff2）: いわゆるコモンシンボル（未初期化グローバル変数の暫定定義）。

`st_shndx` が `SHN_UNDEF` のシンボルこそが、リンカが「定義を探さねばならない」未解決の参照です。

## st_info ―― 型と束縛

`st_info` の 1 バイトには、2 つの情報が詰め込まれています。上位 4 ビットが**束縛** (binding)、下位 4 ビットが**型** (type) です。仕様ではマクロ `ELF64_ST_BIND(info) = info >> 4`、`ELF64_ST_TYPE(info) = info & 0xf` で取り出します。

束縛は、シンボルの「見える範囲」を表します。

| 値 | 名前 | 意味 |
|---|---|---|
| 0 | `STB_LOCAL` | 局所的。このファイル内でのみ見える（`static`） |
| 1 | `STB_GLOBAL` | 大域的。他のファイルから見える |
| 2 | `STB_WEAK` | 弱い大域。同名の `STB_GLOBAL` があればそちらが優先 |

型は、シンボルが指す対象の種類です。

| 値 | 名前 | 意味 |
|---|---|---|
| 0 | `STT_NOTYPE` | 種類未指定 |
| 1 | `STT_OBJECT` | データオブジェクト（変数） |
| 2 | `STT_FUNC` | 関数 |
| 3 | `STT_SECTION` | セクション |
| 4 | `STT_FILE` | ソースファイル名 |

> [!NOTE]
> C の `static` キーワードが、ここで `STB_LOCAL` という形に翻訳されているのが分かります。`static int x;` は他のファイルから見えてはいけないので局所束縛、ただの `int x;` は他から参照できるべきなので大域束縛になります。リンカは大域シンボルだけを名前解決の対象にし、局所シンボルは各ファイル内に閉じて扱います。`STB_WEAK` は、ライブラリが既定の実装を提供しつつ、利用者が同名で上書きできるようにする、といった用途に使われます。

## 再配置 ―― 空欄を埋める指示書

シンボルテーブルは「名前とアドレスの対応表」でした。では、コードの中の「ここに `helper` のアドレスを埋めてほしい」という空欄は、どう表現されるのでしょうか。それが**再配置** (relocation) です。

再配置エントリは、「ファイル（やメモリ）の**この位置**に、**このシンボル**のアドレスを、**この計算方法**で書き込め」というリンカへの指示書です。x86-64 では加数付きの `Elf64_Rela` 形式が使われます。

```c
typedef struct {
    uint64_t r_offset;   /* 書き換える位置 */
    uint64_t r_info;     /* シンボル番号 + 再配置タイプ */
    int64_t  r_addend;   /* 加える定数（補正値） */
} Elf64_Rela;
```

`r_offset` は、書き換えるべきバイト位置です（`.o` の中ではセクション先頭からのオフセット）。`r_info` には 2 つの情報が詰まっており、上位 32 ビットがシンボルテーブルのインデックス（どのシンボルか）、下位 32 ビットが**再配置タイプ**（どう計算するか）です。マクロ `ELF64_R_SYM(info) = info >> 32`、`ELF64_R_TYPE(info) = info & 0xffffffff` で取り出します。

再配置タイプはアーキテクチャ依存で、x86-64 では ABI [](#cite:matz2013) が定めています。代表的なものを挙げます。記号 `S` はシンボルのアドレス、`A` は加数 `r_addend`、`P` は書き換え位置のアドレスです。

| タイプ | 計算式 | 用途 |
|---|---|---|
| `R_X86_64_64` | `S + A` | 64 ビット絶対アドレスを書く |
| `R_X86_64_PC32` | `S + A - P` | 32 ビット相対アドレス（call/jmp など） |
| `R_X86_64_PLT32` | `L + A - P` | PLT 経由の関数呼び出し |

`R_X86_64_PC32` の「`S + A - P`」は、**現在位置 P からシンボル S までの距離**を計算しています。x86-64 の `call` 命令は「次の命令から相対何バイト先へ飛ぶか」で関数を呼ぶため、絶対アドレスではなく相対距離が必要なのです。リンカは、配置が確定して `S` と `P` が定まった時点でこの引き算を行い、`call` 命令のオペランド欄にその差を書き込みます。

> [!TIP]
> 再配置を「数式付きの空欄」と捉えると理解しやすくなります。`.o` の段階では `S`（シンボルの最終アドレス）も `P`（命令の最終アドレス）も未確定なので、計算できません。そこでアセンブラは「ここに `S + A - P` を埋めよ」という指示（再配置エントリ）と、空欄（多くは 0）だけを残します。リンクが進んで全部品の配置が決まると、`S` と `P` が判明し、リンカが実際に引き算して空欄を埋める ―― これが「再配置」という名前の由来です。

## 具体例で追う

簡単な例で、シンボルと再配置がどう現れるかを見ましょう。次の 2 ファイルを考えます。

```c
/* main.c */
extern int helper(int);
int main(void) { return helper(41); }
```

```c
/* helper.c */
int helper(int x) { return x + 1; }
```

`main.c` を `gcc -c main.c` でコンパイルした `main.o` を `readelf` で調べると、`helper` が未定義シンボル（`st_shndx = UND`）として現れ、同時に `.rela.text` に「`call helper` の位置を `R_X86_64_PLT32` で埋めよ」という再配置エントリが現れます。

```
$ readelf -r main.o      # -r は再配置情報
Relocation section '.rela.text' at offset ... contains 1 entry:
  Offset          Info           Type           Sym. Name + Addend
000000000005  000900000004 R_X86_64_PLT32    helper - 4

$ readelf -s main.o      # -s はシンボル
   Num:    Value  Size Type    Bind   Ndx Name
     9: 0000  0    NOTYPE  GLOBAL  UND  helper
```

`helper` の `Ndx`（= `st_shndx`）が `UND`（未定義）であること、再配置エントリの `Sym. Name` が `helper` であることを確認してください。`helper.o` 側では逆に `helper` が `STT_FUNC` の定義済みシンボルとして現れます。リンク時には、リンカが `main.o` の未定義 `helper` を `helper.o` の定義済み `helper` に結びつけ、`R_X86_64_PLT32` の式に従って `call` のオペランドを埋めます。これでようやく `main` から `helper` が呼べるようになるのです。

## 動的シンボルとの違い

ここまでは静的リンク、つまりリンク時にすべてを 1 つにまとめる話でした。しかし `printf` のような標準ライブラリ関数は、実行時に共有ライブラリ `libc.so` から借りてくるのが普通です。この場合のシンボルは、`.symtab` ではなく**動的シンボルテーブル** `.dynsym`（種類 `SHT_DYNSYM`）に置かれ、名前は `.dynstr` に格納されます。

構造体 `Elf64_Sym` 自体は `.symtab` と同じですが、`.dynsym` は実行時にも参照される（`SHF_ALLOC` が立つ）点が異なります。`.symtab` は `strip` で削除でき、削除しても実行には支障ありませんが、`.dynsym` は動的リンクに必須なので削除できません。この実行時の名前解決がどう行われるかは、次章のローディングと動的リンクで扱います。

シンボルと再配置という「名前とアドレスの仲立ち」を理解したので、いよいよ完成した実行ファイルが**メモリに読み込まれて動き出す**過程 ―― ローディングへ進みましょう。これで ELF 部の全体像が完結します。
