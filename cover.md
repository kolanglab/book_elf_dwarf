# ELF/DWARF 入門

**バイナリの中身を読み解く ―― 実行ファイルとデバッグ情報のフォーマット入門**

---

あなたが書いた C のプログラムは、コンパイルされ、リンクされ、最終的に 1 つの「実行ファイル」になります。その実行ファイルをテキストエディタで開くと、ほとんどが意味不明なバイト列に見えるでしょう。しかし、そのバイト列にはきちんとした構造（**フォーマット**）があります。Linux をはじめとする多くの環境では、その構造が **ELF** (Executable and Linkable Format) という規格で定められています。

さらに、デバッガで実行ファイルを止めて変数の中身を覗いたり、クラッシュした行番号を表示したりできるのはなぜでしょうか。機械語には「変数名」も「行番号」もありません。それらを補う情報が、**DWARF** という別のフォーマットで実行ファイルの中に埋め込まれているからです。

本書は、この 2 つのフォーマットを **概要から詳細へ** という流れで解説します。前半は ELF 部、後半は DWARF 部に分かれており、最後に C 言語で小さなツールを実際に作る **ハンズオン** を用意しました。手を動かしながら読むことで、「バイナリの中身は読めば分かる」という感覚を身につけてもらうことが本書の目標です。

> [!NOTE]
> 想定読者は、C 言語で簡単なプログラムが書ける学部 3 年生程度で、コンパイラやリンカといった言語処理系の内部にこれから踏み込もうとしている人です。アセンブリや OS の知識は、必要になったその場で説明します。

> [!IMPORTANT]
> 本書は AI によって生成されたコンテンツです。技術的な内容については、各章で引用している一次資料（とりわけ ELF 仕様 [](#cite:tis1995) と DWARF 仕様 [](#cite:dwarf2017)）にあたって確認することを強くおすすめします。
