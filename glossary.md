# 付録B 用語集

本書に登場した主な用語を、簡潔にまとめます。詳しい説明は、各用語の解説がある本文の章を参照してください。

## ELF 関連

**ELF** (Executable and Linkable Format)
: Linux などで標準的に使われる、オブジェクトファイル・実行ファイル・共有ライブラリの形式。本書第 I 部のテーマ。

**オブジェクトファイル** (object file)
: コンパイル／アセンブル後、リンク前の中間生成物（`.o`）。ELF では `ET_REL`（再配置可能ファイル）。

**リンクビュー / 実行ビュー**
: 同じ ELF ファイルの 2 つの見方。リンクビューは「セクション」の集まりとしてリンカが見る見方、実行ビューは「セグメント」の集まりとしてローダが見る見方（第 2 章）。

**セクション** (section)
: リンクビューにおけるファイルの区分け単位。`.text`（コード）、`.data`（初期値ありデータ）、`.bss`（ゼロ初期化データ）、`.rodata`（読み取り専用データ）など（第 4 章）。

**セグメント** (segment)
: 実行ビューにおける区分け単位。プログラムヘッダで記述され、メモリへの配置と保護属性を定める。複数のセクションを束ねる（第 6 章）。

**文字列テーブル** (string table)
: ヌル終端文字列を連結したセクション。`sh_name` などはこの中へのオフセットで名前を参照する。`.shstrtab`・`.strtab`・`.dynstr` がある（第 4 章）。

**シンボル** (symbol)
: 名前の付いたアドレス。関数や変数の名前と位置の対応。定義済みシンボルと未定義シンボルがある（第 5 章）。

**再配置** (relocation)
: 「この位置にこのシンボルのアドレスをこう計算して書け」というリンカへの指示。名前を最終アドレスに変換する仕組み（第 5 章）。

**ローダ** (loader)
: 実行ファイルをメモリに読み込み、実行を開始させる OS の機能。`PT_LOAD` セグメントの指示に従う（第 6 章）。

**動的リンク** (dynamic linking)
: 実行時に共有ライブラリを結びつけること。動的リンカ（`ld-linux`）が `.dynamic` の情報をもとに行う（第 6 章）。

**PIE** (Position Independent Executable)
: どのアドレスにも配置できる実行ファイル。ASLR を可能にする。ELF 上は `ET_DYN`（第 3 章）。

**GOT / PLT**
: 動的リンクで外部関数・変数を間接参照するためのテーブル。読み取り専用コードから書き換え可能なアドレスを参照する仕組みで、遅延束縛を実現する（第 6 章）。

**ABI** (Application Binary Interface)
: コンパイル済みバイナリどうしが協調動作するための取り決め。オブジェクト形式・呼び出し規約・データ配置などを含む（第 1 章）。

## DWARF 関連

**DWARF**
: デバッグ情報の標準フォーマット。変数名・型・行番号などを ELF の `.debug_*` セクションに格納する。本書第 II 部のテーマ（第 8 章）。

**DIE** (Debugging Information Entry)
: DWARF の基本単位。タグ・属性・子で構成され、木構造をなす。関数・変数・型などすべてを表す（第 7・8 章）。

**タグ / 属性** (`DW_TAG_*` / `DW_AT_*`)
: DIE が何を表すかを示すのがタグ、その性質を表すのが属性（第 8 章）。

**コンパイル単位** (compilation unit, CU)
: 1 つのソースファイルに対応する DWARF 情報のまとまり。`.debug_info` は CU の集まり（第 9 章）。

**略号** (abbreviation)
: DIE の雛形（タグと属性の並び）を `.debug_abbrev` に一度だけ定義し、`.debug_info` では番号で参照する仕組み。情報をコンパクトにする（第 9 章）。

**LEB128** (Little Endian Base 128)
: 可変長の整数符号化。小さい整数を少ないバイトで表す。DWARF の随所で使われる（第 9 章）。

**場所式** (location expression)
: 変数が「どこにあるか」を表す、スタックマシンのプログラム。`DW_OP_*` の並びを実行するとアドレス（やレジスタ）が得られる（第 10 章）。

**ロケーションリスト** (location list)
: 変数の場所がアドレス範囲ごとに変わる場合の記述。最適化で変数の居場所が移動する状況に対応する（第 10 章）。

**行番号プログラム** (line number program)
: `.debug_line` に格納された、アドレスと行の対応表を生成するバイトコード。状態機械で実行する（第 11 章）。

**行番号状態機械** (line number state machine)
: 行番号プログラムを実行する仮想マシン。`address`・`line` などのレジスタを増分命令で更新し、行を出力する（第 10・12 章）。

**コールフレーム情報** (Call Frame Information, CFI)
: 任意のアドレスで呼び出し元フレームをどう復元するかを記述する情報。`.debug_frame` / `.eh_frame` に格納され、バックトレースを支える（第 12 章）。

**`<optimized out>`**
: 最適化により、その瞬間に変数の値がどこにも保持されていない状態。DWARF が正直にロケーションリストの「穴」として伝えた結果（第 12 章）。
